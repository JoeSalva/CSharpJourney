// Ternary Operators assign values to a variable if the conditions are met.
// The syntax is: variable = (condition) ? expressionTrue : expressionFalse;

// int x = 10;

// String result = x > 5
//     ? "x is greater than 5"
//     : "x is less than 5";
// Console.WriteLine(result);

